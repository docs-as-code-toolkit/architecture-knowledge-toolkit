interface:
  display_name: "Post-Merge Sync"
  short_description: "Update local checkout after PR merge"
  default_prompt: "Use $post-merge-sync after a pull request has been merged to switch back to <base-branch>, fast-forward the latest changes, and clean up the merged local branch if safe."
